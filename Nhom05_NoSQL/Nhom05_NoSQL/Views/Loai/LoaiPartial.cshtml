@model IEnumerable<Nhom05_NoSQL.Models.Loai>

<ul class="list-group list-group-flush text-left">
    <li class="list-group-item">
        <a href="#" style="text-transform:uppercase; text-decoration:none; color:#FA748F; font-weight:bold; font-size:17px">DANH MỤC SẢN PHẨM</a>
    </li>
    @{
        foreach (var item in Model)
        {
            <li class="list-group-item" style="">
                <a href="@Url.Action("SanPhamTheoLoai", "Loai", new { @maloai = item.MaLoai})" style="text-transform:uppercase;text-decoration:none">
                    @item.TenLoai
                </a>
            </li>
        }

    }
</ul>